{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["@playwright/mcp@latest", "--browser", "chrome", "--storage-state", ".playwright-mcp/auth.json", "--secrets", ".playwright-mcp/secrets.env", "--output-dir", ".playwright-mcp"]
    }
  }
}
